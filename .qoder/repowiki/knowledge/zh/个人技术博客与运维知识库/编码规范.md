- 文章 URL Slug 优先使用 Frontmatter 中的 `abbrlink` 字段，若未设置则通过文件名哈希算法生成 8 位短链接。
- 文章有效日期采用多级回退策略：优先使用 `updated` 字段，其次对比文件物理修改时间（mtime），最后回退到 `date` 字段。
- 在首页列表和文章详情页根据发布日期自动计算时效性，对超过 3 个月或 1 年的文章分别显示黄色或红色过时警告徽章。
- 所有页面均嵌入结构化数据（JSON-LD），并在 `BaseLayout` 中统一配置 Open Graph 与 Canonical 链接以优化 SEO。