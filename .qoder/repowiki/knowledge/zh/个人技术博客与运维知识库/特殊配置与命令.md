- **AI 摘要生成**：需在 `.env` 中配置 `VOLCENGINE_API_KEY` 或 `GEMINI_API_KEY`，执行 `npm run predev` 或 `npm run prebuild` 触发 `scripts/summarize.mjs` 自动为缺失摘要的文章生成内容。
- **快速创作**：运行 `npm run new -- "文章标题"` 调用 `scripts/new-post.mjs` 自动生成带标准 Frontmatter 的 Markdown 文件。